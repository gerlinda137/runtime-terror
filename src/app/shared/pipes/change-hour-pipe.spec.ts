import { ChangeHourPipe } from './change-hour-pipe';

describe('ChangeHourPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeHourPipe();
    expect(pipe).toBeTruthy();
  });
});
