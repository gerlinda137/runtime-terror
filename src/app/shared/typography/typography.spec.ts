import { Typography } from './typography';

describe('Typography', () => {
  it('should create an instance', () => {
    const directive = new Typography();
    expect(directive).toBeTruthy();
  });
});
