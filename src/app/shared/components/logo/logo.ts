import { Component } from '@angular/core';
import { Typography } from '../typography/typography';

@Component({
  selector: 'app-logo',
  imports: [Typography],
  templateUrl: './logo.html',
  styleUrl: './logo.scss',
})
export class Logo { }
