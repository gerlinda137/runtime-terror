import { CryptoIcon } from './crypto-icon';

describe('CryptoIcon', () => {
  it('should create an instance', () => {
    const directive = new CryptoIcon();
    expect(directive).toBeTruthy();
  });
});
